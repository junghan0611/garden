---
title: "#LLM: python sympy orgmode"
author: ["Junghan Kim"]
date: 2024-12-11T12:03:00+09:00
tags: ["llmlog"]
draft: false
---

## [2024-12-11]({{< relref "/journal/20241211T000000.md" >}}) {#2024-12-11--relref-journal-20241211t000000-dot-md}


### <span class="org-todo todo TODO">TODO</span> [SUM]: {#sum}


#### @user  sympy를 조직모드에서 호출하는 방법? {#user-sympy를-조직모드에서-호출하는-방법}


#### @assistant  SymPy를 Org-mode에서 호출하려면, Emacs에서 Python 코드를 실행할 수 있는 방법을 사용해야 합니다. 다음은 그 방법입니다: {#assistant-sympy를-org-mode에서-호출하려면-emacs에서-python-코드를-실행할-수-있는-방법을-사용해야-합니다-dot-다음은-그-방법입니다}

1.  **Python 코드 블록 사용**:

    -   Org-mode에서는 `#+BEGIN_SRC=와 =#+END_SRC` 사이에 Python 코드를 작성할 수 있습니다.

    <!--listend-->

    ```org
       #+BEGIN_SRC python :results output :exports both
       from sympy import *
       x = symbols('x')
       expr = sin(x) ** 2 + cos(x) ** 2
       print(simplify(expr))
    ```
    \#+end_src

2.  **Python 코드 블록 실행**:
    -   작성한 코드 블록을 실행하려면, 커서를 코드 블록 안에 두고 `C-c C-c` (또는 `M-x org-babel-execute-src-block`)를 누릅니다.

3.  **결과 보기**:
    -   코드가 실행되면, 그 결과가 바로 아래에 나타납니다.

4.  **SymPy 설치 확인**:
    -   SymPy가 설치되어 있는지 확인하세요. 없다면, Emacs 내에서 `M-x shell=을 실행한 후 =pip install sympy` 명령어로 설치할 수 있습니다.

5.  **Org-mode 설정**:

    -   Org-mode에서 Python 코드 블록을 실행하려면, `org-babel-load-languages` 설정이 필요합니다. `.emacs` 또는 `init.el` 파일에 다음 코드를 추가하세요:

    <!--listend-->

    ```elisp
       (org-babel-do-load-languages
        'org-babel-load-languages
        '((python . t)))
    ```

이렇게 하면 Org-mode에서 SymPy를 사용하여 수학적 계산을 쉽게 수행할 수 있습니다.


#### @user {#user}


## ㅇㅇ {#ㅇㅇ}

```python
   from sympy import *
```
