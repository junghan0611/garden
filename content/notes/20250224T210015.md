---
title: "#둠이맥스 Warning (emacs): Org version mismatch."
author: ["Junghan Kim"]
date: 2025-02-24T21:00:00+09:00
tags: ["doomemacs", "llmlog"]
categories: ["Noname"]
draft: false
---

## History {#history}

-   <span class="timestamp-wrapper"><span class="timestamp">[2025-04-02 Wed 10:17] </span></span> 도 아닌 도. 그냥 해결 되는 문제 아닌 문제
-   <span class="timestamp-wrapper"><span class="timestamp">[2025-02-24 Mon 21:00] </span></span> 아주 귀찮게 하는 문제


## 2025 해결 방법 {#2025-해결-방법}

<span class="timestamp-wrapper"><span class="timestamp">[2025-04-02 Wed 10:16] </span></span> 도 아닌 도. 방법 없이 되는 도

둠이맥스 경우, 빌트인 안쓴다. 빌트인 elc 파일 삭제하고. 이맥스 켜놓고 기다려라. 네이티브 컴파일 비동기로 쭉 할 때까지.


### 빌트인 org's elc 파일 삭제 {#빌트인-org-s-elc-파일-삭제}

<span class="timestamp-wrapper"><span class="timestamp">[2025-03-25 Tue 13:36]</span></span>

예를 들어, 다음과 같다.

```shell

# cd /usr/share/emacs/30.1/lisp/org
# sudo rm -Rf *.elc

# cd /usr/local/stow/emacs/lib/emacs/30.1.50/
cd /usr/local/stow/emacs/share/emacs/30.1.50/lisp/org
sudo rm -Rf *.elc
```


## 2025 작업로그: 어떻게 했는가? 그래서? {#2025-작업로그-어떻게-했는가-그래서}


### config.el 의미 있나? {#config-dot-el-의미-있나}

넣었다. 위에

```elisp
(straight-use-package 'org)
```


### <span class="org-todo done DONT">DONT</span> delete elc for org {#delete-elc-for-org}

elc 삭제한다.

```shell

cd /home/junghan/.emacs.d/eln-cache
rm -Rf 29.4-517f4e77

cd ~/doomemacs-junghan0611/.local/cache
# rm -Rf eln-cache
cd .local/straight
rm -Rf build-30.0.93/org or build-29.4.50/org
rm -Rf repos/org

cd ~/doomemacs-junghan0611/.local/cache
rm -Rf comp eln
```


### delete native-comp-eln-load-path {#delete-native-comp-eln-load-path}

```shell
rm -Rf /home/junghan/doomemacs-git/.local/cache/eln/
```

<div class="NOTES">

native-comp-eln-load-path is a variable defined in comp.c. Value ("_home/junghan/doomemacs-git_.local/cache/eln/" "_tmp/doom.run_.emacs.d/eln-cache/" "_usr/local/stow/emacs/lib/emacs/30.1.50/native-lisp_")

</div>


### doom sync {#doom-sync}

<span class="timestamp-wrapper"><span class="timestamp">[2025-03-04 Tue 10:31]</span></span>

```shell

doom sync
```


## [2025-02-24 W08]({{< relref "/journal/20250224T000000.md" >}}) {#2025-02-24-w08--relref-journal-20250224t000000-dot-md}

@user 다음 문제를 어떻게 해결할까?

⛔ Warning (emacs): Org version mismatch. This warning usually appears when a built-in Org version is loaded prior to the more recent Org version.

Version mismatch is commonly encountered in the following situations:

1.  Emacs is loaded using literate Org config and more recent Org version is loaded inside the file loaded by ‘org-babel-load-file’. ‘org-babel-load-file’ triggers the built-in Org version clashing the newer Org version attempt to be loaded later.

    It is recommended to move the Org loading code before the ‘org-babel-load-file’ call.

2.  New Org version is loaded manually by setting ‘load-path’, but some other package depending on Org is loaded before the ‘load-path’ is configured. This "other package" is triggering built-in Org version, again causing the version mismatch.

    It is recommended to set ‘load-path’ as early in the config as possible.

3.  New Org version is loaded using straight.el package manager and other package depending on Org is loaded before straight triggers loading of the newer Org version.

    It is recommended to put

    (straight-use-package 'org)

    early in the config. Ideally, right after the straight.el bootstrap. Moving ‘use-package’ :straight declaration may not be sufficient if the corresponding ‘use-package’ statement is deferring the loading.

4.  A new Org version is synchronized with Emacs git repository and stale .elc files are still left from the previous build.

    It is recommended to remove .elc files from lisp/org directory and re-compile

@assistant **\*Org 버전 불일치 경고 해결 방법**

1.  **Literate Org Config 사용 시**:
    -   `org-babel-load-file` 호출 전에 Org 로딩 코드를 이동하세요.

2.  **수동으로 load-path 설정 시**:
    -   =load-path=를 설정 파일의 초기에 설정하세요. 다른 Org 의존 패키지가 먼저 로드되지 않도록 주의하세요.

3.  **straight.el 패키지 관리자 사용 시**:
    -   =(straight-use-package 'org)=을 설정 파일의 초기에, straight.el 부트스트랩 직후에 추가하세요.

4.  **Emacs Git 저장소와 동기화 시**:
    -   이전 빌드에서 남은 `.elc` 파일을 제거하고 다시 컴파일하세요.

<!--listend-->

```elisp
;; 설정 파일 초기에 load-path 설정
(setq load-path (cons "/path/to/org-mode/lisp" load-path))
(setq load-path (cons "/path/to/org-mode/contrib/lisp" load-path))
(require 'org)

;; straight.el 사용 시
(straight-use-package 'org)
```

이러한 방법으로 Org 버전 불일치 경고를 해결할 수 있습니다.

Citations: [1] <https://edykim.com/ko/post/start-with-orgmode-basics/> [2] <https://list.orgmode.org/87fsgh5uec.fsf@localhost/t/> [3] <https://seorenn.github.io/note/doom-emacs-troubleshooting.html> [4] <https://lists.gnu.org/archive/html/bug-gnu-emacs/2023-04/msg00586.html> [5] <https://hillfolk.org/posts/org-mode-set-up-dir/>

@user


## 아카이빙 {#아카이빙}
