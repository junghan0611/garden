[[redirects]]
  from = "/static/*"
  to = "/static/:splat"
  status = 200
  force = false

[build]
  publish = "public"
  command = "npx quartz build && cp -r static/* public/static/ 2>/dev/null || true"

[build.environment]
  NODE_VERSION = "22"