[[redirects]]
  from = "/static/*"
  to = "/static/:splat"
  status = 200
  force = false

[build]
  publish = "public"
  command = "npm run build && mkdir -p public/static && cp -r static/* public/static/ || true"

[build.environment]
  NODE_VERSION = "22"