[[redirects]]
  from = "/static/*"
  to = "/static/:splat"
  status = 200
  force = false

[build]
  publish = "public"
  command = "npm run build && cp -r static/* public/static/"

[build.environment]
  NODE_VERSION = "22"